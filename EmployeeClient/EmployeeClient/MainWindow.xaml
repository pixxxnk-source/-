<Window x:Class="EmployeeClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="社員管理システム"
        Height="450"
        Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 入力エリア -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="名前：" VerticalAlignment="Center"/>
            <TextBox x:Name="NameTextBox" Width="120" Margin="5,0"/>

            <TextBlock Text="部署：" VerticalAlignment="Center"/>
            <TextBox x:Name="DepartmentTextBox" Width="120" Margin="5,0"/>

            <TextBlock Text="入社日：" VerticalAlignment="Center"/>
            <DatePicker x:Name="HireDatePicker" Width="150" Margin="5,0"/>
        </StackPanel>

        <!-- ボタン -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="追加" Width="80" Margin="5"
                    Click="AddButton_Click"/>
            <Button Content="更新" Width="80" Margin="5"
                    Click="UpdateButton_Click"/>
            <Button Content="削除" Width="80" Margin="5"
                    Click="DeleteButton_Click"/>
            <Button Content="再読込" Width="80" Margin="5"
                    Click="ReloadButton_Click"/>
        </StackPanel>

        <!-- 一覧 -->
        <DataGrid x:Name="EmployeeDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"/>
    </Grid>
</Window>

